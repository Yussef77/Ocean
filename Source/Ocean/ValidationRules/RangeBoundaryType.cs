namespace Oceanware.Ocean.ValidationRules {

    /// <summary>Represents the values for the enum RangeBoundaryType.</summary>
    public enum RangeBoundaryType {

        /// <summary>
        /// Use inclusive logic when testing values, similar to greater than equal and less than equal.
        /// </summary>
        Inclusive,

        /// <summary>
        /// Use exclusive logic when testing values, similar to greater than and less than.
        /// </summary>
        Exclusive
    }
}
